# 📘 교과서 기반 인터랙티브 수학 웹 애플리케이션 제작 규칙
(Textbook-to-Interactive Math Web App Generation Rules)

본 문서는 **중1 수학(좌표평면과 그래프: `g1_coordinate.html`)** 구현 사례를 바탕으로, 교과서 지면(이미지, 텍스트, 단원 내용)이 주어졌을 때 **영서중학교 디지털 수학 인터랙티브 웹 앱으로 자동 변환·개발하기 위한 핵심 아키텍처 및 상세 제작 표준**입니다.

---

## 1. 🧭 전체 개발 철학 및 변환 원칙

1. **지면 교과서의 완벽한 1:1 대화형 인터랙션화**:
   - 교과서의 모든 코너(되짚어보기, 생각열기, 본문 개념, 함께하기, 문제, 스스로 확인하기, 생각 넓히기)를 누락 없이 1:1 서브스텝(`SubStep`)으로 분할 매핑합니다.
   - 정적인 그림/도표/문제를 **학생이 직접 조작(드래그, 클릭, 슬라이더, 펜 드로잉)하고 즉각 피드백을 받는 동적 캔버스(Two.js / Canvas 2D)**로 전환합니다.
2. **5단계 탐구 순환(Inquiry Cycle) 구조 준수**:
   - `0. 되짚어보기` $\rightarrow$ `1. 생각열기/개념` $\rightarrow$ `2. 성질/정당화` $\rightarrow$ `3. 형성평가(스스로 확인하기)` $\rightarrow$ `4~5. 심화/생각 넓히기`
3. **독립 실행형 싱글 파일 번들(Single File Bundle)**:
   - 각 단원 페이지(`g1_coordinate.html` 등)는 단일 파일로 완전 동작하며, LMS 연동 SDK, KaTeX, Two.js, Web Audio API를 내장합니다.

---

## 2. 📚 교과서 코너별 웹 인터랙션 매핑 표준

| 교과서 코너 | 웹 서브스텝 역할 | 좌측 캔버스 인터랙션 (Canvas) | 우측 인터랙티브 카드 (Instruction & Form) |
|---|---|---|---|
| **되짚어보기 (선수학습)** | 이전 학년/학기 선수 지식 점검 (3문항 구성) | • 대응 표 Canvas<br>• 수직선 정수/유리수 틱마크 Canvas<br>• 식의 값 시각화 | • 인라인 빈칸 채우기<br>• 통과 시 1단원 잠금(🔒) 즉시 해금 |
| **생각열기 (도입 & 실생활)** | 실생활 맥락에서 수학적 필요성/규칙성 직관적 발견 | • 도로변 건물 위치와 수직선<br>• 자석과 안내문 이어붙이기<br>• 놀이기구 대기열 시뮬레이터 | • 자유 서술형 질문(`textarea`, 무채점 LMS 저장)<br>• 규칙성 발견 표/문장 빈칸 채우기 |
| **개념 학습 (원리 정립)** | 정의, 기호, 성질의 엄밀한 수학화 | • **동적 점 드래그 실습** (점 P를 움직이면 $P(x, y)$ 라벨 실시간 갱신)<br>• 사분면 영역 파스텔 컬러링<br>• 축과 원점 시각화 | • 핵심 용어/기호 인라인 빈칸<br>• **[오개념 바로잡기 / 핵심 팁]** 주의 카드 제공 (예: 영문 대문자 O와 숫자 0 구분 등) |
| **문제 / 예제 (기본 적용)** | 개념 적용 및 조작 미션 | • 캔버스 위 점 위치 확인<br>• **목표 좌표로 점 드래그 미션**<br>• **격자 모눈 클릭하여 점 찍기(Plotting)** | • 점의 좌표 기호 입력 (`P(a, b)`)<br>• 사분면 번호 입력 (`제( 2 )사분면`) |
| **스스로 확인하기 (형성평가)** | 1~5번 문항 단계별 마스터 | • 1번: 수직선/좌표 기호 읽기<br>• 2번: 점 4개 목표 위치 동시 배치<br>• 3~4번: 사분면/부호 판정<br>• 5번: 좌표축 위의 점과 넓이 계산 | • 단답형 / 다중 체크박스(ㄱ~ㄹ) / 단계별 풀이 빈칸<br>• **태블릿 펜 드로잉 레이어 연동** (보조선 직접 그리기) |
| **생각 넓히기 (심화/융합)** | 실생활 연계, 게임형 챌린지 미션 | • 보물지도 시각화 + 펜으로 좌표축 그리기 + 보물 위치 터치 지정 | • 단서 해석 및 조건 추론<br>• 최종 미션 완수 축하 카드 |

---

## 3. 📐 좌표평면 및 기하 캔버스 구성 상세 규칙 (Canvas Engine)

### 3.1 좌표평면 그리드(Grid) 설계 규격
- **그리드 간격(Step)**: 기본 `step = 30px` 또는 `40px` 사용.
- **중심점(Center) 자동 정렬**:
  ```javascript
  const width = two.width, height = two.height;
  const step = 40;
  const centerX = Math.round(width / 2 / step) * step;
  const centerY = Math.round(height / 2 / step) * step;
  ```
- **좌표축 및 화살표**:
  - 메인 $x$축, $y$축: 굵기 `2.5px`, 색상 `#1e293b`.
  - 화살표: $x$축 우측 끝, $y$축 상단 끝에 뾰족한 삼각형 패스(`two.makePath`) 렌더링.
  - 축 라벨: $x$는 우측 끝 아래(이탤릭체, 18px), $y$는 상단 끝 왼쪽(이탤릭체, 18px).
  - 원점 $O$: 교점 좌하단(`cx - 14, cy + 14`)에 영문 대문자 `O` 표기 (숫자 0과 구분).
- **격자선(Grid Lines)**:
  - 1단위 보조 격자선: 굵기 `1px`, 색상 `#cbd5e1` 또는 `#e2e8f0`.
  - 눈금 수치 라벨: 짝수(-4, -2, 2, 4 등) 또는 주요 정수 눈금에 또렷한 텍스트(`#475569`, 13~14px) 표기.

### 3.2 사분면(Quadrant) 시각화 규칙
- **영역별 파스텔톤 배경색**:
  - 제1사분면 ($x>0, y>0$): 연주황/오렌지 `rgba(254, 215, 170, 0.40)`
  - 제2사분면 ($x<0, y>0$): 연보라/퍼플 `rgba(233, 213, 255, 0.40)`
  - 제3사분면 ($x<0, y<0$): 연초록/그린 `rgba(187, 247, 208, 0.40)`
  - 제4사분면 ($x>0, y<0$): 연하늘/블루 `rgba(186, 230, 253, 0.40)`
- **정답 스포일러 방지 원칙**:
  - 사분면 뱃지(상단 모서리 라벨)에는 '제1사분면'과 같은 **명칭만 표기**하고, 부호($(+, +)$ 등)는 미리 노출하지 않음. 학생이 탐구 후 직접 부호 표를 채우도록 설계.

### 3.3 인터랙티브 점(Point) 및 드래그 조작 규칙
1. **점 마커 가시성**:
   - 메인 점: 반경 7.5~8px 원, 채우기 테마색(`#4f46e5` 등), 외곽선 흰색(2px).
   - 터치 유도 아우라(Aura): 반경 16px, 반투명 테마색(`rgba(79, 70, 229, 0.15)`), 펄스 효과.
2. **수선의 발 가이드라인(Dashed Guide Lines)**:
   - 점 $P$에서 $x$축, $y$축으로 수선의 발 점선(`dashes: [4, 4]`, 굵기 1.5px)을 연결하여 현재 좌표를 직관적으로 판독 가능하게 구현.
3. **스마트 자석 스냅 (Magnetic Snapping)**:
   - 마우스/터치 위치를 격자 정수 단위로 즉시 반올림 스냅:
     ```javascript
     let rx = Math.round((mx - cx) / step);
     let ry = Math.round((cy - my) / step);
     // 좌표 제한 (캔버스 범위 초과 방지)
     rx = Math.max(-maxGrid, Math.min(maxGrid, rx));
     ry = Math.max(-maxGrid, Math.min(maxGrid, ry));
     ```
4. **다중 점 드래그 및 목표 검증**:
   - 복수 점(A, B, C, D)을 각각 드래그하여 목표 좌표에 배치하는 문항에서는 각 점의 목표 도달 여부를 실시간 뱃지(`점 A 완료!`, `점 B 배치하기`)로 즉각 피드백.

### 3.4 캔버스 클릭 점 찍기 (Plotting System)
- 순서쌍 점을 직접 찍는 단계(예: $4.3-2$)에서는 캔버스 클릭 이벤트를 통해 가장 가까운 격자점에 점을 추가/토글:
  - 이미 찍힌 점을 다시 누르면 삭제.
  - 현재 찍힌 점 개수를 실시간 인디케이터로 표시 (`3개 / 5개`).

### 3.5 투명 펜 드로잉 오버레이 레이어 (Drawing Canvas)
- 도형 작도, 보조선 긋기, 보물지도 탐구 등 필기가 필요한 단계(`2-7`, `2-8`)에서는 Two.js 캔버스 위에 `<canvas id="freehand-drawing-canvas">`를 투명 오버레이.
- 상단 툴바에 색상 선택(파랑, 빨강, 검정), 지우개, 드로잉 삭제 버튼 제공.
- 필요 없는 서브스텝에서는 자동으로 캔버스를 숨기고 마우스/터치 이벤트를 점 드래그 전용으로 격리.

---

## 4. 🔲 빈칸(Blank / Input) 설계 및 채점·검증 규칙

### 4.1 빈칸이 뚫려야 하는 5대 핵심 영역
1. **핵심 수학 개념 및 용어**:
   - 예: `좌표`, `순서쌍`, `x축`, `y축`, `좌표축`, `원점`, `좌표평면`, `x좌표`, `y좌표`, `변수`, `그래프`, `정비례`, `반비례`.
2. **수학적 기호 및 표준 표기법**:
   - 예: 점의 좌표 기호 `P(a)`, `P(a, b)`, 원점 기호 `(0, 0)`, 개별 점 좌표 `A(-5)`, `B(1)`.
3. **사분면 명칭 및 부호**:
   - 예: `제1사분면`, `제2사분면`, `제3사분면`, `제4사분면` 및 부호 `+`, `-`.
4. **수학적 불변량, 관계성, 비교 키워드**:
   - 예: "(132, 37)과 (37, 132)는 서로 ( 다르다 )", "항상 x좌표를 y좌표 ( 앞 )에 쓴다", "좌표축 위의 점은 어느 사분면에도 ( 속하지 않는다 )", "$x$가 증가하면 $y$도 ( 증가 )한다".
5. **단계별 연산 및 최종 해**:
   - 예: 표의 대응 수치(`12, 15, 18`), 수직선 대응 유리수(`-7/2` 또는 `-3.5`), 방정식의 해($a+3=0 \implies a=-3$), 도형의 넓이.

### 4.2 인풋 필드 UI 스타일 가이드
```html
<!-- 문맥 속 인라인 괄호 빈칸 -->
• 점 A의 좌표: ( <input type="text" id="p13-a" class="proof-input-text" style="width:120px;" placeholder="좌표 기호 입력"> )
```
- **클래스**: 반드시 `.proof-input-text` 클래스를 적용하여 통일된 디자인 유지.
- **적정 너비(Width)**:
  - 부호(`+`, `-`): `45px~55px` (가운데 정렬)
  - 단순 수치/짧은 단어: `65px~85px`
  - 순서쌍 기호(`P(a, b)`): `100px~130px`
  - 긴 설명형 빈칸: `120px~150px`
- **가이드 플레이스홀더 및 정답 미노출 원칙 (Zero Answer Leakage)**:
  - `placeholder="수 입력"`, `placeholder="용어 입력"`, `placeholder="좌표 기호 (예: P(a,b))"` 등 **입력 형식 가이드만 제시**.
  - **🚫 정답 직접 노출 절대 금지**: 해당 문제의 실제 정답(숫자, 계산값, 정답 텍스트)을 placeholder나 라벨 예시로 직접 노출하는 것은 학습 효과를 저해하므로 절대 금지합니다.
  - 서브에이전트 검증 루프에서 모든 placeholder의 정답 누출 여부를 상시 점검합니다.

### 4.3 채점 및 텍스트 정규화 엔진 (`normTxt`)
모든 텍스트 검증은 공백 및 대소문자 차이로 인한 억울한 오답을 방지하기 위해 정규화 함수를 필히 통과시킵니다:
```javascript
function normTxt(v) {
  return v ? v.trim().replace(/\s+/g, '').toUpperCase() : '';
}
```
- **유연한 정답 인정 규칙**:
  - **분수/소수 동시 허용**: `-7/2`, `-3.5`, `-7분의2` 모두 정답 처리.
  - **한국어 어미 변화 허용**: `다르다`, `다름`, `달라진다` $\rightarrow$ `.includes('다르') || .includes('다름')`.
  - **괄호 및 기호 표기 유연성**: `P(-4, 2)` 채점 시 `P(-4,2)`, `(-4,2)`, `P(-4, 2)` 등 유연한 정규식 또는 부분 포함 허용.
- **캔버스 조작과의 결합 검증**:
  - 텍스트 입력만 맞추는 것이 아니라, 캔버스 위의 점이 목표 위치로 이동했는지(`state.pointP.x === 2`) 동시 판정.

### 4.4 피드백 및 정답 확인 화면 전환
1. **오답 처리**:
   - `.proof-error-notice` 인라인 경고창 출력.
   - 단순 "틀렸습니다"가 아닌, **어느 부분을 다시 생각해보아야 하는지 구체적인 힌트(`hintMsg`)** 제공.
2. **정답 처리 (`renderVerifiedAnswerView`)**:
   - 정답 즉시 `.verified-answer-card`로 전환.
   - 수학적으로 정돈된 완벽한 해설 요약 제시.
   - [🚀 다음 단계로 진행하기 ➔] 버튼으로 자연스럽게 다음 서브스텝 이동.

---

## 5. ⚙️ 데이터 상태 관리 및 영속화 규칙 (State & LMS)

### 5.1 전역 State 스키마
```javascript
const state = {
  studentId: '10101',
  studentName: '학생',
  isTeacherLoggedIn: false,
  currentMainTab: 0,
  subStep: '1-1',
  tool: 'select',
  unlockedTabs: [0],
  unlockedSubSteps: ['0-1'],
  completedSubSteps: [],
  pointP: { x: 3, y: 2, label: 'P' },
  savedFormInputs: {},          // 단계별 작성 중인 인풋 값 자동 저장 캐시
  verifiedViewData: {}          // 완료된 단계의 정답 화면 캐시
};
```

### 5.2 답안 자동 저장 및 복원
- 학생이 다른 탭을 둘러보고 돌아왔을 때 입력했던 텍스트나 조작 상태가 초기화되지 않도록 `saveCurrentFormInputs()` 및 `restoreFormInputs()`를 필수 탑재.
- 실시간 타이핑 중 1초 디바운스로 LMS에 학습 진행률 동기화 (`attachRealtimeInputTracker()`).

### 5.3 교사 관리자 모드
- **마스터 패스워드**: `260523`, `260831`
- 교사 모드 진입 시:
  - 5x5 관제실 실시간 대시보드 (`render5x5StudentGridG1`).
  - [🔓 학생 해금 범위 설정]: 학생들의 진도 상한선을 교사가 원격 제어.
  - [🔑 교사 패스 (Pass)]: 수업 시연 중 즉시 다음 단계 강제 통과.

---

## 6. 🧩 교과서 요소를 웹 인터랙션으로 변환하는 8대 핵심 디자인 패턴

모든 단원(1, 2, 3, 5, 6, 7, 8단원) 개발 시 교과서 4단원(`g1_coordinate.html`) 벤치마크에서 확립된 다음 **8대 변환 공식**을 기계적으로 적용합니다:

```
[패턴 1] 정적 삽화/다이어그램 ───> Two.js 벡터 애니메이션 및 기하 렌더러
[패턴 2] 본문 개념 설명 ─────────> 실시간 드래그 & 스마트 자석 스냅 인터랙터
[패턴 3] 좌표/위치 찾기 문항 ────> 다중 점 목표 배치 미션 & 실시간 상태 뱃지
[패턴 4] 보조선/작도형 문항 ─────> 투명 펜 드로잉 오버레이 레이어
[패턴 5] 개념 정의 및 수식 ─────> 인라인 괄호 빈칸 (.proof-input-text) & KaTeX
[패턴 6] 오개념/주의사항 ────────> 스포일러 방지형 [주의 / 핵심 팁] 카드
[패턴 7] 제출 및 정답 확인 ─────> 폭죽(Confetti) + 음향 + 해설 요약 카드 전환
[패턴 8] 교과서 흐름/페이지 ─────> 상단 대단원 탭 + 하단 서브스텝 필(Pills)
```

1. **패턴 1 (정적 삽화 $\rightarrow$ Two.js 동적 캔버스)**: 교과서의 그림을 캡처하지 않고 순수 Two.js 코드로 직접 그려 Retina 해상도와 동적 애니메이션 지원.
2. **패턴 2 (본문 개념 $\rightarrow$ 실시간 드래그 & 스마트 자석 스냅)**: 수학적 대상을 잡고 움직이며 변화를 체득. 격자 정수 단위 자동 반올림 스냅(`Math.round((mx - cx) / step)`) 적용.
3. **패턴 3 (좌표/위치 찾기 $\rightarrow$ 다중 점 목표 배치 미션)**: 목표 위치로 점/객체를 옮기면 실시간 뱃지(`완료!`)로 게이미피케이션 피드백.
4. **패턴 4 (보조선/작도 $\rightarrow$ 투명 펜 드로잉 오버레이)**: Two.js 위에 투명 캔버스 레이어를 얹어 태블릿 펜이나 마우스로 보조선 긋기 및 지우기 지원.
5. **패턴 5 (개념/수식 $\rightarrow$ 인라인 괄호 빈칸 & KaTeX)**: 문맥 속 `.proof-input-text` 빈칸과 `normTxt` 정규화 채점으로 공백/기호 유연 처리.
6. **패턴 6 (오개념/주의 $\rightarrow$ 스포일러 방지 팁 카드)**: placeholder나 힌트에 정답을 직접 노출하지 않고 입력 형식만 안내 (Zero Answer Leakage).
7. **패턴 7 (제출 $\rightarrow$ 축하 및 해설 카드 전환)**: 정답 시 경쾌한 사운드 + 색종이 폭죽(`launchConfetti()`) + 깔끔한 정답 해설 카드(`.verified-answer-card`) 전환.
8. **패턴 8 (교과서 위계 $\rightarrow$ 잠금 해금 내비게이션)**: 이전 단계를 완료해야 다음 서브스텝이 열리는 빗장 구조(`🔒`)와 교사 마스터 키(`260523`, `260831`).

---

## 7. ⚡ 프레임 기반 물리 애니메이션 및 화면 고정 방지 표준 규격

시뮬레이터 조작 후 서브스텝 전환 시 화면이 멈추거나 캔버스가 이전 단계로 강제 고정되는 현상을 원천 차단하기 위해 다음 표준을 엄격히 적용합니다:

### 7.1 `startSmoothLerp` 수렴 안전 가드
- 매 프레임 `getter()`를 재호출하지 않고 내부 부동소수점 변수(`currentFloat`)를 유지하여 지수 감속을 계산합니다.
- 변화량이 임계치 미만일 때 최종 목표값(`targetVal`)으로 즉시 강제 안착하고 `requestAnimationFrame`을 완전히 종료합니다:
```javascript
function startSmoothLerp(key, getter, setter, targetVal, onFrame, onComplete, speed = 0.12) {
  if (typeof activeLerpAnimations === 'undefined') window.activeLerpAnimations = {};
  if (activeLerpAnimations[key]) {
    cancelAnimationFrame(activeLerpAnimations[key]);
    delete activeLerpAnimations[key];
  }
  let currentFloat = getter();
  function step() {
    const diff = targetVal - currentFloat;
    if (Math.abs(diff) < 0.02 || Math.abs(diff * speed) < 0.005) {
      setter(targetVal);
      if (onFrame) onFrame(targetVal);
      if (onComplete) onComplete();
      delete activeLerpAnimations[key];
      return;
    }
    currentFloat += diff * speed;
    setter(currentFloat);
    if (onFrame) onFrame(currentFloat);
    activeLerpAnimations[key] = requestAnimationFrame(step);
  }
  activeLerpAnimations[key] = requestAnimationFrame(step);
}
```

### 7.2 `loadSubStep` 진입 시 잔류 애니메이션 전량 정리
서브스텝을 변경할 때 실행 중인 모든 애니메이션 루프를 즉시 취소하여 새 페이지 캔버스 오염을 방지합니다:
```javascript
if (typeof activeLerpAnimations !== 'undefined') {
  for (const k in activeLerpAnimations) {
    if (activeLerpAnimations[k]) {
      cancelAnimationFrame(activeLerpAnimations[k]);
      delete activeLerpAnimations[k];
    }
  }
}
```

### 7.3 시뮬레이터 콜백 내 현재 서브스텝 가드
- 시뮬레이터 컨트롤러 콜백에서 `state.subStep === expectedCode`를 확인하여, 다른 화면에 있을 때 이전 시뮬레이터 캔버스가 다시 그려지는 것을 방지합니다.

---

## 8. 🤖 서브에이전트 검증 규칙 및 8대 무조건 반려(REJECT) 원칙

서브에이전트가 단원 페이지를 자동 평가할 때 다음 8대 필수 탈락 기준 중 **단 하나라도 위반하면 전체 완성도 점수와 무관하게 즉시 반려(`REJECT`)**합니다:

1. **[REJECT-01] 학생 로그인 실패**:
   - 학번 `10101`, 이름 입력 후 활동 뷰로 정상 전환되지 않거나 `0-1` 서브스텝이 열리지 않을 때.
2. **[REJECT-02] 교사 마스터 비밀번호(260523, 260831) 인증 실패**:
   - `student-id`에 `260523` 또는 `260831` 입력 시 즉시 전체 해금 프리패스 실패 시.
   - `🔑 교사 계정 접속` 버튼 클릭 시 `#secure-password-modal`이 출현하지 않거나, 모달에서 `260523` 또는 `260831` 입력 후 관리자 모드 진입에 실패할 때.
3. **[REJECT-03] 정답 미노출 원칙 위반 (Zero Answer Leakage)**:
   - 모든 `.proof-input-text`의 `placeholder`, 주변 안내문구, 레이블 등에 해당 문제의 **실제 채점 정답(숫자, 수식, 텍스트)이 직접 노출**되었을 때.
4. **[REJECT-04] 좌측 캔버스 비어있음(Blank Canvas)**:
   - 특정 서브스텝에서 좌측 캔버스 드로잉 함수가 누락되었거나 런타임 오류가 발생하여 빈 화면으로 방치될 때.
5. **[REJECT-05] 화면 고정 버그 (Screen Freeze) 및 잔류 애니메이션**:
   - 시뮬레이터 버튼 조작 후 다른 서브스텝으로 이동 시 좌측 캔버스가 전환되지 않고 이전 시뮬레이터 화면으로 고정될 때.
   - `loadSubStep`에 `activeLerpAnimations` 취소 로직이 누락되었을 때.
6. **[REJECT-06] 소단원 '스스로 확인하기' 1문항 1페이지 원칙 위반**:
   - 각 소단원 말미의 '스스로 확인하기'에 수록된 문항들을 한 페이지에 다수 합산하여 작성하고 1문제당 1페이지로 독립 분할하지 않았을 때.
7. **[REJECT-07] 대단원 '스스로 마무리하기' 전용 대주제 탭 미분리 및 1문항 1페이지 원칙 위반**:
   - 대단원 권말의 '스스로 마무리하기'가 별도의 독립 대주제 탭으로 분리되지 않고 타 소단원/프로젝트와 혼합되었거나, 수록 문항이 문제 1개당 1페이지로 분할되지 않았을 때.
8. **[REJECT-08] 기준 페이지(g1_coordinate.html) 대비 질적 완성도 미달**:
   - Two.js 동적 그래픽, KaTeX 수식 렌더링, 4대 모달, 5x5 관제 대시보드, normTxt 채점 UX, 5종 사운드 등 기준 규격 대비 달성률이 90% 미만일 때.

---

## 9. 📋 타 단원 개발 지시 및 검증 11대 체크리스트

1. [ ] **지면 분할 무손실성**: 교과서 지면의 생각열기, 개념정리, 문제, 스스로 확인하기, 생각넓히기가 빠짐없이 1:1 서브스텝으로 분할 매핑되었는가?
2. [ ] **소단원 '스스로 확인하기' 1문항 1페이지**: 소단원별 스스로 확인하기의 모든 문항이 문항당 1개의 독립 서브스텝(Two.js 시뮬레이터+폼)으로 분할되었는가?
3. [ ] **대단원 '스스로 마무리하기' 전용 탭 독립**: 권말 스스로 마무리하기가 타 단원과 섞이지 않고 별도 대주제 탭으로 단독 편성되었는가?
4. [ ] **대단원 '스스로 마무리하기' 1문항 1페이지**: 스스로 마무리하기의 전 문항(기본, 표준, 발전/서술형)이 문제 1개당 1페이지(1 서브스텝)로 완전 분할되었는가?
5. [ ] **좌측 캔버스 비어있음 0건**: 어떤 서브스텝에서도 좌측 캔버스가 빈 화면으로 방치되지 않고 의미 있는 그래픽이나 시뮬레이터가 작동하는가?
6. [ ] **동적 조작성(Interactivity)**: 정적인 그림 감상이 아니라, 학생이 슬라이더, 드래그, 클릭 조작을 할 수 있는 인터랙션이 최소 1개 이상 포함되었는가?
7. [ ] **수식 KaTeX 표준화**: 모든 수학 기호, 변수, 수식에 `$x$`, `$\overline{AB}$`, `$y=ax$` 등의 LaTeX 문법이 적용되었는가?
8. [ ] **정답 미노출 원칙(Zero Answer Leakage)**: 모든 인풋의 `placeholder`에 실제 정답이 누출되지 않았는가?
9. [ ] **부드러운 애니메이션 수렴**: `startSmoothLerp`를 사용할 때 부동소수점 추적(`currentFloat`)과 임계 스냅 조건이 적용되어 무한 루프가 발생하지 않는가?
10. [ ] **페이지 이동 시 애니메이션 취소**: `loadSubStep` 진입 시 `activeLerpAnimations`를 일괄 취소하여 화면 고정 버그를 예방하였는가?
11. [ ] **2대 교사 마스터 비밀번호 지원 & Git 즉시 동기화**: `260523` 및 `260831` 프리패스 및 모달이 작동하며, 작업 완료 즉시 원격 동기화가 이루어졌는가?

---

## 10. 📝 소단원 '스스로 확인하기' 1문항 1페이지 상세 표준 (1 Question = 1 Substep)
1. **분할 원칙**: 교과서 각 소단원 말미에 배치된 '스스로 확인하기'(보통 4~6문항 구성)는 절대로 한 화면에 2개 이상의 문항을 병합하지 않습니다.
2. **독립된 UX**: 각 문항마다 번호(`[1.1 확인 1]`, `[1.1 확인 2]` 등)를 명시하고, 좌측에는 해당 문항의 수학적 시각화/인터랙티브 캔버스를, 우측에는 해당 문항 전용 입력폼 및 채점 피드백을 배치합니다.
3. **독립 채점 및 해설 카드**: 각 문항 제출 시 개별 유효성 검사, Confetti 축하, 그리고 해당 문항 전용 정답 해설 카드로 전환되어야 합니다.

---

## 11. 🏆 대단원 '스스로 마무리하기' 전용 탭 편성 및 1문항 1페이지 상세 표준
1. **대주제 탭 분리**: 대단원 권말의 '스스로 마무리하기'(통상 10~15문항)는 본문 소단원 탭(예: 1~4단원)과 완전히 분리된 **별도의 대주제 탭(예: Tab 5. 스스로 마무리하기)**으로 단독 개설합니다.
2. **전 문항 1문항 1페이지 완전 매핑**: 기본 문제, 표준 문제, 발전/서술형 문제를 가리지 않고 교과서의 문항 번호(01번~14번 등)를 1:1로 서브스텝(`5-1` ~ `5-14`)에 배정합니다.
3. **창의융합 프로젝트 별도 탭 분리**: 스스로 마무리하기 뒤에 이어지는 교과서 '창의융합 프로젝트'는 스스로 마무리하기 탭에 합치지 않고, 별도의 후속 탭(예: Tab 6. 창의융합 프로젝트)으로 분리 구성합니다.


