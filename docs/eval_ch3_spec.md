# 📋 [설계 명세서] 3단원 문자와 식 (g1_ch3_equations.html)

본 문서는 **Redbook 메인 에이전트**가 제작한 중학교 1학년 수학 3단원 **[문자와 식]** 대화형 웹 애플리케이션의 개발 의도와 검증 기준을 정의한 명세서입니다.  
**독립 서브에이전트**는 본 명세서를 기반으로 실제 브라우저/DOM 환경에서 기능들을 전수 검증하며, **90% 이상의 달성도**를 기록할 때 최종 통과(`PASS`) 판정을 부여합니다.

---

## 🎯 1. 단원 개요 및 교육과정 매핑
- **교과서 단원**: 2022 개정 중학교 1학년 수학 3단원 **Ⅲ. 문자와 식** (교과서 68쪽 ~ 103쪽)
- **핵심 교육 목표**:
  1. 문자를 사용하여 수량 관계를 간단한 식으로 나타내고 식의 값을 구할 수 있다.
  2. 일차식의 개념을 이해하고, 분배법칙과 동류항 모으기를 통해 일차식의 덧셈과 뺄셈을 능숙하게 계산할 수 있다.
  3. 등식의 성질을 양팔 저울 모델을 통해 직관적으로 이해하고, 방정식과 항등식을 구분할 수 있다.
  4. 이항(移項)의 원리를 이해하고 일차방정식을 해결하며, 실생활 문제에 적용할 수 있다.
  5. 대수적 모델링(수학 마술 트릭 등)을 통해 문자와 식의 유용성을 체험한다.
  6. **프레임 기반 부드러운 물리 애니메이션 (Lerp 감속)**: 수치 조작 시 즉각적인 순간이동 대신 지수 감속(`dx * 0.12`)으로 우아하게 이동하며, 목표 도달 시 루프를 즉각 종료(`cancelAnimationFrame`)하여 전력과 CPU를 보존.

---

## 🧩 2. 핵심 설계 의도 (Intents) 및 검증 항목

### [INTENT-01] 🔐 3중 보안 로그인 및 단계적 잠금 해금 시스템
- **01-A (기본 차단)**: 로그인 전에는 `#main-workspace`가 은닉(`display:none`)되고 `#login-modal`이 표출되어야 함.
- **01-B (학생 로그인)**: 5자리 학번(`10101` 등)으로 로그인 성공 시 `0-1` 서브스텝만 기본 해금되고 이후 단계는 `locked-pill` 상태여야 함.
- **01-C (교사 마스터 바이패스)**: 교사 마스터 비밀번호(`260523` 또는 `260831`) 입력 시 즉시 관리자 권한이 부여되고 전체 19개 서브스텝이 프리패스로 전체 해금되어야 함.
- **01-D (교사 계정 접속 버튼 및 모달 - 필수)**:
  - 상단 헤더의 `🔑 교사 계정 접속` (`#btn-open-teacher-login`) 클릭 시 교사 로그인 모달(`teacher-login-modal`)이 `display:flex`로 팝업되어야 함.
  - 비밀번호 입력 후 승인 시 즉시 교사 모드로 전환되고 모달이 닫혀야 함.
  - **이 항목이 동작하지 않으면 전체 완성도와 무관하게 즉시 반려(`REJECT`)함**.

### [INTENT-02] 🟡 노란색 정답 빈칸 표준 UX (`#fef08a`)
- 모든 답안 입력창(`.proof-input-text`)은 미입력 시 부드러운 노란색 배경(`background-color: #fef08a`)을 유지하여 학생이 입력해야 할 위치를 명확히 안내해야 함.
- 포커스 시 테두리 하이라이트 및 흰색 배경으로 전환되어야 함.

### [INTENT-03] 🪟 4대 모달 시스템 완비
- `g1_coordinate.html`과 100% 동일하게 다음 4대 모달이 탑재되어야 함:
  1. `#curriculum-map-modal` (단원 맵)
  2. `#learning-goals-modal` (학습 목표)
  3. `#teacher-login-modal` (교사 비밀번호 인증)
  4. `#teacher-range-modal` (학생 해금 범위 일괄 설정)

### [INTENT-04] 0-1 초등 복습: [□를 사용한 식과 수평 저울] (교과서 68쪽)
- 좌측 캔버스에 왼쪽 접시($3 \times \square + 5$)와 오른쪽 접시($20$)의 양팔 저울 시각화.
- $\square$ 값을 변경하면 저울의 기울기가 동적으로 계산되어 평형을 이룰 때 수평 렌더링.
- `check01Submit()`에서 $\square = 5$ 채점 정상 동작.

### [INTENT-05] 1-1 교과서 70쪽 생각열기: [아이스크림 구매 영수증 시뮬레이터]
- 1개 3500원인 아이스크림의 수량 $x$를 조절하는 슬라이더 인터랙션.
- 슬라이더 조작 시 바코드, 개수 $x$, 단가 3500원, 총액 $3500x$ 원이 실시간 영수증에 인쇄되는 애니메이션.
- `check11Submit()`에서 $3500x$, $3500a + 5000b$ 문장 식 채점 정상 동작.

### [INTENT-06] 1-3 교과서 74쪽 생각열기: [독서 진행 게이지와 식의 값]
- 400쪽 책을 하루에 10쪽씩 $x$일 읽었을 때 남은 쪽수 $400 - 10x$ 시뮬레이터.
- 일수 $x$ 슬라이더를 조작하면 Two.js 게이지 바에서 읽은 쪽수(파란색)와 남은 쪽수(오렌지색)가 동적으로 갱신.
- `check13Submit()`에서 $400 - 10x$ 및 $x=5$일 때 $350$쪽 채점 정상 동작.

### [INTENT-07] 2-1 교과서 76~77쪽: [다항식 구조 카드 분해기]
- 다항식 $-3x^2 + 5x - 7$을 각 항 카드($-3x^2$, $+5x$, $-7$)로 분해하여 시각화.
- 계수, 상수항, 최고차수를 버튼으로 하이라이트 가능.
- `check21Submit()`에서 계수(5), 상수항(-7), 차수(2), 일차식 판별(예) 채점 정상 동작.

### [INTENT-08] 2-2 교과서 78쪽: [직사각형 면적 모델과 분배법칙]
- 단항식과 수의 곱셈 $2x \times 3 = 6x$ 및 분배법칙 $3(x + 2) = 3x + 6$을 Two.js 직사각형 분할 면적으로 렌더링.
- 가로/세로 길이 변경 시 면적 조각들이 시각적으로 분할 및 합체.
- `check22Submit()`에서 $6x$, $-8x + 12$, $2x - 3$ 채점 정상 동작.

### [INTENT-09] 2-3 교과서 80쪽: [동류항 대수 막대 타일 모으기]
- $x$ 타일(녹색 긴 막대)과 $1$ 타일(노란색 작은 정사각형)을 인터랙티브하게 배치.
- [동류항 모으기] 클릭 시 같은 타일끼리 정렬되고 합산($6x - 2$)되는 Two.js 모션.
- `check23Submit()`에서 동류항 판별 및 합 계산($6x - 2$) 채점 정상 동작.

### [INTENT-10] 3-1 교과서 84쪽: [방정식 vs 항등식 판별 저울]
- $x$ 슬라이더를 조절할 때, 항등식($2x+3x=5x$)은 어떤 $x$에서도 항상 완벽한 평형을 유지하고, 방정식($2x=6$)은 오직 $x=3$에서만 찰칵 수평을 맞추는 인터랙티브 저울.
- `check31Submit()`에서 방정식과 항등식 분류 채점 정상 동작.

### [INTENT-11] 3-3 교과서 88~89쪽: [등식의 성질 양팔 저울 실험실]
- 양팔 저울의 좌변($x-3$)과 우변($5$)에 대하여 [+3 더하기], [-3 빼기], [×2 곱하기], [÷2 나누기] 버튼 제공.
- 양변에 동일 연산 수행 시 수평이 유지되면서 최종적으로 $x=8$로 단순화되는 과정 시뮬레이션.
- `check33Submit()`에서 등식의 성질에 따른 연산 수치 채점 정상 동작.

### [INTENT-12] 4-1 교과서 92쪽: [이항(移項) 애니메이션 시뮬레이터]
- 방정식 $5x - 2 = 2x + 3$에서 [이항 실행]을 누르면 우변의 $+2x$가 좌변으로 포물선을 그리며 $-2x$로 부호가 바뀌고, 좌변의 $-2$가 우변으로 넘어가며 $+2$로 바뀌는 모션.
- 이항 결과 $3x = 5$가 도출되는 시각화.
- `check41Submit()`에서 이항 후 식($3x = 5$) 및 부호 반전 원리 채점 정상 동작.

### [INTENT-13] 5-2 교과서 102쪽 창의융합: [생각한 수 맞추기 수학 마술사]
- 학생이 임의의 수 $x$를 정하고 마술 단계($+4 \rightarrow \times 2 \rightarrow -6 \rightarrow \div 2 \rightarrow -x$)를 실행하면 항상 결과가 1로 귀결되는 대수 마술.
- 카드가 뒤집히며 대수식 변형 과정($(2x+2)/2 - x = 1$)이 시각적으로 증명됨.
- `check52Submit()`에서 최종 결과(1) 및 $x$ 소거 원리 채점 정상 동작.

### [INTENT-14] 🌟 중1 좌표평면(g1_coordinate.html) 대비 질적 완성도 및 8대 디자인 변환 패턴 준수
- 영서중 기준 페이지인 `g1_coordinate.html` 및 [교과서 4단원 대조 보고서](file:///home/ubuntu/workspace/Redbook/docs/textbook_to_coordinate_reflection_report.md)의 **8대 인터랙션 변환 패턴**을 3단원에 1:1 대응 적용:
  - **패턴 1 (정적 삽화 $\rightarrow$ Two.js 동적 캔버스)**: 성냥개비 도형 배열, 양팔 저울 수평/기울임, 이항 포물선 궤적 벡터 그래픽.
  - **패턴 2 (본문 개념 $\rightarrow$ 실시간 드래그 & 스마트 자석 스냅)**: 저울 위 추 드래그 & 양변 연산, 식의 값 슬라이더 실시간 수렴.
  - **패턴 3 (좌표/위치 $\rightarrow$ 목표 배치 미션)**: 동류항 끼리 모으기 바구니 분류 및 일차식 계산 단계별 미션.
  - **패턴 4 (보조선/작도 $\rightarrow$ 투명 펜 드로잉 오버레이)**: 복잡한 방정식 풀이 단계 밑줄 및 화살표 필기 레이어.
  - **패턴 5 (개념/수식 $\rightarrow$ 인라인 괄호 빈칸 & KaTeX)**: 문자 식 표기($5x-2$), 일차방정식의 해($x=3$) 인라인 빈칸과 `normTxt` 채점.
  - **패턴 6 (오개념/주의 $\rightarrow$ 스포일러 방지 팁 카드)**: 곱셈/나눗셈 기호 생략 규칙, 분배법칙 부호 실수 주의 등.
  - **패턴 7 (제출 $\rightarrow$ 축하 및 해설 카드 전환)**: Confetti 폭죽 + 음향 + 정답 해설 카드(`.verified-answer-card`).
  - **패턴 8 (교과서 흐름 $\rightarrow$ 잠금 해금 내비게이션)**: 19개 서브스텝 잠금(`🔒`) 및 2대 교사 마스터 키.
- 기준 페이지 대비 질적 지표(Two.js 동적 캔버스, 4대 모달, 5x5 대시보드, 교과서 1:1 서브스텝 실험실, LMS 동기화, normTxt 채점 UX, 5종 사운드 시스템) **90% 이상 필수 일치**.

### [INTENT-15] 🚫 정답 미노출 원칙 (Zero Answer Leakage in Placeholder/Hints)
- 학생이 정답을 입력하는 빈칸의 `placeholder`, 안내 문구, 힌트 예시 등에 **해당 문제의 실제 정답(숫자, 수식, 텍스트 등)을 절대 직접 노출하지 않음**.
- `placeholder`는 오직 입력 형식 안내(예: `placeholder="일차식 입력 (예: ax+b)"`, `placeholder="해 (숫자)"`, `placeholder="방정식 또는 항등식"`) 목적으로만 구성.
- 모든 입력란의 플레이스홀더를 전수 감사하여 실제 정답 누출 발견 시 즉시 반려.

### [INTENT-16] ⚙️ 절전형 물리 애니메이션 엔진 및 화면 고정 방지 표준
- 전 단원 공통 물리 애니메이션 함수 `startSmoothLerp(key, getter, setter, targetVal, onFrame, onComplete, speed = 0.12)` 탑재.
- 내부 실수 변수(`currentFloat`) 기반 지수 감속 수렴 및 임계 도달(`|diff| < 0.02 || |diff*speed| < 0.005`) 시 즉시 강제 안착 및 `requestAnimationFrame` 완전 종료.
- **`loadSubStep` 진입 시 `activeLerpAnimations` 잔여 애니메이션 전량 취소** (`cancelAnimationFrame` 호출 후 삭제)하여 페이지 전환 시 이전 시뮬레이터로 인한 좌측 캔버스 고정 현상 100% 차단.
- 시뮬레이터 콜백 내 `state.subStep === expectedCode` 가드로 오염 방지.

### [INTENT-17] 🔑 2대 교사 마스터 비밀번호 동시 지원 (`260523`, `260831`)
- 학번/비밀번호 로그인 및 `#secure-password-modal`에서 `260523`과 `260831` 모두 정상적으로 교사 관리자 권한을 획득하고 전 서브스텝이 프리패스 해금되어야 함.

---


### [INTENT-RULE10] 📄 소단원 '스스로 확인하기' 1문항 1페이지 원칙
- 각 소단원 말미의 '스스로 확인하기'에 수록된 모든 문항은 결코 한 화면에 병합하지 않고, 문제 1개당 독립된 1개 서브스텝(1페이지)으로 분할하여 전용 시뮬레이터와 채점 폼을 배정해야 함.

### [INTENT-RULE11] 🏆 대단원 '스스로 마무리하기' 전용 대주제 탭 및 1문항 1페이지 원칙
- 대단원 권말의 '스스로 마무리하기'는 타 소단원과 섞이지 않고 별도의 독립 대주제 탭으로 단독 편성해야 함.
- 수록된 모든 문항을 문제 1개당 1페이지(1 서브스텝)로 1:1 완전 매핑해야 하며, 창의융합 프로젝트는 후속 탭으로 분리해야 함.

## 📊 3. 서브에이전트 통과 및 판정 기준 (8대 무조건 반려 원칙)

서브에이전트는 다음 8대 필수 탈락 기준 중 **단 하나라도 위반 시 무조건 반려(`REJECT`)**합니다:

1. **[REJECT-01] 학생 로그인 실패**:
   - 학번 `10101`, 이름 입력 후 활동 뷰로 정상 전환되지 않거나 `0-1`이 열리지 않을 때.
2. **[REJECT-02] 2대 교사 마스터 비밀번호(260523, 260831) 및 모달 실패**:
   - `260523` 또는 `260831` 마스터 비밀번호 바이패스 실패 시.
   - `🔑 교사 계정 접속` 버튼 클릭 시 모달 미출현 또는 모달 내 인증 실패 시.
3. **[REJECT-03] 정답 미노출 원칙 위반 (Zero Answer Leakage)**:
   - `placeholder` 또는 안내 라벨에 실제 문제의 정답이 직접 노출되었을 때.
4. **[REJECT-04] 좌측 캔버스 비어있음 (Blank Canvas)**:
   - 3단원 19개 서브스텝 중 단 하나의 서브스텝이라도 캔버스 함수 미실행 또는 런타임 오류로 빈 화면이 발생할 때.
5. **[REJECT-05] 화면 고정 버그 (Screen Freeze) 및 잔류 애니메이션**:
   - 시뮬레이터 조작 후 서브스텝 이동 시 좌측 캔버스가 이전 단계 화면으로 고정될 때.
   - `loadSubStep` 내 `activeLerpAnimations` 취소 로직 누락 시.
6. **[REJECT-06] 소단원 '스스로 확인하기' 1문항 1페이지 원칙 위반**:
   - 각 소단원 말미의 '스스로 확인하기' 문항들을 한 화면에 묶어 작성하고 1문제당 1페이지로 독립 분할하지 않았을 때.
7. **[REJECT-07] 대단원 '스스로 마무리하기' 전용 탭 미분리 및 1문항 1페이지 원칙 위반**:
   - 대단원 권말 '스스로 마무리하기'가 별도 탭으로 분리되지 않았거나, 전 문항 1문항 1페이지로 분할되지 않았을 때.
8. **[REJECT-08] 질적 완성도 90% 미달**:
   - `g1_coordinate.html` 대비 Two.js 인터랙션, KaTeX 수식, 모달 관제 등 종합 질적 지표가 90% 미만일 때.

### [종합 합격 기준]
- 8대 필수 항목 100% 통과 및 전체 검증 항목 90% 이상 성공 시 최종 합격(`PASS`), 미달 시 반려(`REJECT`).

