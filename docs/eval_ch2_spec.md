# 📋 [설계 명세서] 2단원 정수와 유리수 (g1_ch2_integers.html) 제작 의도 및 기대 동작

- **작성자**: Redbook 메인 에이전트
- **평가 대상**: `g1_ch2_integers.html`
- **기준 문서**: 교과서(`textbook/textbook.pdf`) 2단원 및 `g1_coordinate.html` 아키텍처

---

## 🎯 1. 핵심 아키텍처 및 기본 기능 설계 의도 (Essential Architecture)
- **프레임 기반 부드러운 물리 애니메이션 (Lerp 감속)**: 수치 조작 시 즉각적인 순간이동 대신 지수 감속(`dx * 0.12`)으로 우아하게 이동하며, 목표 도달 시 루프를 즉각 종료(`cancelAnimationFrame`)하여 전력과 CPU를 보존.

### [INTENT-01] 🔐 로그인 모듈 및 교사 권한 (필수 탈락 기준)
- **INTENT-01-A (로그인 UI)**: 학번 인풋(`student-id`), 비밀번호 인풋(`student-name`), 로그인 버튼, 교사 계정 접속 버튼(`openTestLoginModal`)이 모두 정상 배치되어야 함.
- **INTENT-01-B (학생 로그인)**: 학번 `10101`, 비밀번호 입력 후 로그인 시 활동 뷰(`view-activity`)로 전환되며, `0-1`은 해금(🔓), 이후 단계는 잠금(🔒) 상태여야 함.
- **INTENT-01-C (마스터 비밀번호 바이패스)**: 학번 또는 비밀번호에 마스터 번호 `260523` 또는 `260831` 입력 시 교사 관리자 모드로 진입하고 전체 서브스텝이 프리패스 해금되어야 함.
- **INTENT-01-D (교사 계정 접속 버튼 및 모달 팝업)**: [필수 평가 항목] `🔑 교사 계정 접속` 버튼 클릭 시 아무 반응이 없거나 런타임 오류가 발생하면 안 되며, `#secure-password-modal`이 화면에 `display: flex`로 즉시 나타나야 함. 이후 모달 내에서 `260523` 또는 `260831` 입력 후 확인 시 교사 관리자 모드로 즉시 로그인되어야 함.
- **중대 실패 기준 (무조건 반려)**: 학생 로그인, 마스터 바이패스, 또는 **교사 계정 접속 버튼 클릭/모달 팝업** 중 단 하나라도 실패할 경우, 전체 완성도 점수와 상관없이 **무조건 반려(REJECT)**.

### [INTENT-02] 🟡 노란색 정답 입력 빈칸 규격
- 정답 입력 필드(`.proof-input-text`, `.proof-input-textarea`)는 커서를 놓기 전 노란색 배경(`background-color: #fef08a`, 테두리 `2px solid #eab308`)을 유지해야 함.
- 커서 포커스 시 흰색(`background-color: #ffffff`, 테두리 `#3b82f6`)으로 전환되어야 함.

### [INTENT-03] 🏛️ 4대 모달 및 관리 시스템
- `#secure-password-modal`: 교사 비밀번호 인증 모달 존재.
- `#unlock-boundary-modal`: 학생 진도 범위 해금 모달 존재.
- `#teacher-dashboard-modal`: 실시간 학생 제출 현황 모달 존재.
- `#student-zoom-modal`: 1:1 학생 확대 관제 모달 존재.
- 상단 헤더에는 불필요한 별도 교사 버튼이 없어야 함.

---

## 🔬 2. 교과서 1:1 연계 인터랙티브 시뮬레이터 설계 의도 (Interactive Simulators)

### [INTENT-04] 0-1, 0-2 되짚어보기 (초등 수직선 및 연산)
- `0-1`: 분수 덧셈(`1/3 + 1/2 = 5/6`)과 소수 곱셈(`0.4 * 0.5 = 0.2`) 채점 로직 정상 동작.
- `0-2`: 수직선 눈금 읽기 (점 A = 3, 점 B = 1.5) 채점 및 핀 배치 동작.

### [INTENT-05] 1-1 교과서 50쪽 생각열기: [로봇 위치 이동 시뮬레이터]
- `setupSubstepSimulator`를 통해 `#interactive-sim-controller`에 로봇 조작판이 활성화되어야 함.
- `moveRobot(1)` 실행 시 `simState.robotPos`가 +1 증가하고, 상단 뱃지 텍스트가 갱신되어야 함.
- `moveRobot(-1)` 실행 시 -1 감소, `resetRobot()` 실행 시 원점(0)으로 복귀해야 함.
- `runRobotMission()` 실행 시 교과서 미션인 "+3m 전진 후 -5m 후진 ➔ 최종 -2m" 궤적이 순차적으로 실행되어야 함.
- `check11Submit()`에서 최종 위치 `-2`, 영하 `-7`, 손해 `-500` 정답 판정 정상 동작.

### [INTENT-06] 1-2 & 1-3 교과서 52~53쪽: [수직선 점 마그네틱 배치기]
- 점 A, B, C를 버튼(`setPointP12`)으로 각각 목표 좌표(+3, +1.5, -2.5, -3.5)에 배치할 수 있어야 함.
- `simState.p12Points` 객체에 좌표가 저장되고 캔버스에 깃발과 수치가 렌더링되어야 함.
- `check12Submit()`에서 정수 목록과 0의 성질 채점, `check13Submit()`에서 유리수 O와 -2.5 채점 정상 동작.

### [INTENT-07] 1-4 교과서 54~55쪽: [원점 거리(절댓값) 측정 줄자]
- `setAbsPos(val)` 함수를 호출하여 점 P의 위치를 변경할 수 있어야 함.
- 점 P가 `-5`일 때 원점으로부터의 거리 `5` 및 절댓값 `|-5| = 5`가 디스플레이(`#abs-readout`)와 캔버스에 시각화되어야 함.
- `check14Submit()`에서 $|-5|=5$, $|+2.7|=2.7$, 절댓값 4인 수(+4, -4) 채점 정상 동작.

### [INTENT-08] 2-1 교과서 56~57쪽: [수직선 대소 비교 저울]
- `setCompare(a, b)`를 통해 두 수의 대소 판정 (`+3 > -4`, `-5 < -2`)이 디스플레이(`#compare-readout`)에 갱신되어야 함.
- `check21Submit()`에서 부등호 `>, <, <` 정답 판정 정상 동작.

### [INTENT-09] 3-1 교과서 60~62쪽: [양(+)·음(-) 칩 상쇄 실험실]
- `addChip('blue')`, `addChip('red')`로 칩을 추가하고 카운터(`#chip-counter`)가 업데이트되어야 함.
- `cancelChipPairs()` 호출 시 `simState.chipCancelled = true`가 되며, 쌍 상쇄 후 남은 칩과 계산 공식(`(+3)+(-5)=-2`)이 도출되어야 함.
- `resetChips(b, r)`로 교과서 예제 상태로 재설정 가능해야 함.
- `check31Submit()`에서 덧셈 4문항(+10, -13, +6, -6) 채점 정상 동작.

### [INTENT-10] 3-2 교과서 61쪽: [수직선 2단계 화살표 덧셈기]
- `stepArrow(step)`을 통해 1단계(0에서 +3 발사), 2단계(-5 발사), 3단계(최종 -2 깃발)가 순차적으로 시각화되어야 함.
- `check32Submit()`에서 교환법칙, 결합법칙, 계산값(+5) 채점 정상 동작.

### [INTENT-11] 4-1 교과서 68~70쪽 생각열기: [속도×시간 곱셈 시뮬레이터]
- `setMultSim(v, t)`를 통해 속도와 시간을 변경하고 `mult-readout`에 `(-3) × (-2) = +6`이 갱신되어 음수×음수=양수 증명 가능해야 함.
- `check41Submit()`에서 (+12, -30, +56) 채점 정상 동작.

### [INTENT-12] 5-2 교과서 76~77쪽 프로젝트: [세계 도시 기온차 수직선 온도계]
- `setThermCities(a, b)`를 통해 두 도시(서울 +5, 모스크바 -12)의 수은주와 기온차(17℃)가 렌더링되어야 함.
- `check52Submit()`에서 기온차 `17` 채점 및 대단원 완료 화면(`renderVerifiedAnswerView`) 호출 정상 동작.

### [INTENT-13] 📝 채점 및 피드백 전환
- 모든 채점 함수는 `normTxt` 정규화를 통과하여 공백 및 형식 차이에 유연해야 함.
- 정답 제출 시 `renderVerifiedAnswerView`가 실행되어 녹색 축하 카드와 [🚀 다음 단계로 진행하기 ➔] 버튼이 나타나야 함.

### [INTENT-14] 🌟 중1 좌표평면(g1_coordinate.html) 대비 질적 완성도 및 8대 디자인 변환 패턴 준수
- **비교 대상 기준**: 영서중 대표 기준 페이지인 `g1_coordinate.html` 및 [교과서 4단원 대조 보고서](file:///home/ubuntu/workspace/Redbook/docs/textbook_to_coordinate_reflection_report.md)
- **8대 인터랙션 변환 패턴의 2단원 1:1 적용**:
  - **패턴 1 (정적 삽화 $\rightarrow$ Two.js 동적 캔버스)**: 로봇 이동 도로망, 수직선 유리수 눈금, 대소 비교 저울 벡터 그래픽.
  - **패턴 2 (본문 개념 $\rightarrow$ 실시간 드래그 & 스마트 자석 스냅)**: 수직선 위 점 드래그 및 정수/소수 눈금 자석 스냅, 원점 거리(절댓값) 측정 줄자.
  - **패턴 3 (좌표/위치 $\rightarrow$ 목표 배치 미션)**: 점 A, B, C, D를 목표 유리수 위치로 드래그 배치하는 미션.
  - **패턴 4 (보조선/작도 $\rightarrow$ 투명 펜 드로잉 오버레이)**: 수직선 덧셈/뺄셈 화살표 및 부호 이동 필기 레이어.
  - **패턴 5 (개념/수식 $\rightarrow$ 인라인 괄호 빈칸 & KaTeX)**: 양수/음수 기호, 절댓값 기호 `$|-5|=5$`, 분수/소수 인라인 빈칸과 `normTxt` 채점.
  - **패턴 6 (오개념/주의 $\rightarrow$ 스포일러 방지 팁 카드)**: 0은 양의 정수도 음의 정수도 아님, 음수의 절댓값 비교 주의 등.
  - **패턴 7 (제출 $\rightarrow$ 축하 및 해설 카드 전환)**: Confetti 폭죽 + 음향 + 정답 해설 카드(`.verified-answer-card`).
  - **패턴 8 (교과서 흐름 $\rightarrow$ 잠금 해금 내비게이션)**: 19개 서브스텝 단계적 잠금(`🔒`) 및 2대 교사 마스터 키.
- **통과 기준**: 좌표평면 대비 질적 완성도 지표 **90% 이상** 필수 달성.

### [INTENT-15] 🚫 정답 미노출 원칙 (Zero Answer Leakage in Placeholder/Hints)
- 학생이 정답을 입력하는 빈칸의 `placeholder`, 안내 문구, 힌트 예시 등에 **해당 문제의 실제 정답(숫자, 수식, 텍스트 등)을 절대 직접 노출하지 않음**.
- `placeholder`는 오직 입력 형식 안내(예: `placeholder="부호와 수"`, `placeholder="분수 입력 (예: a/b)"`, `placeholder="기호 입력 (예: >, <)"`) 목적으로만 구성.
- 모든 입력란의 플레이스홀더를 전수 감사하여 실제 정답 누출 발견 시 즉시 반려.

### [INTENT-16] ⚙️ 절전형 물리 애니메이션 엔진 및 화면 고정 방지 표준
- 전 단원 공통 물리 애니메이션 함수 `startSmoothLerp(key, getter, setter, targetVal, onFrame, onComplete, speed = 0.12)` 탑재.
- 내부 실수 변수(`currentFloat`) 기반 지수 감속 수렴 및 임계 도달(`|diff| < 0.02 || |diff*speed| < 0.005`) 시 즉시 강제 안착 및 `requestAnimationFrame` 완전 종료.
- **`loadSubStep` 진입 시 `activeLerpAnimations` 잔여 애니메이션 전량 취소** (`cancelAnimationFrame` 호출 후 삭제)하여 페이지 전환 시 이전 시뮬레이터로 인한 좌측 캔버스 고정 현상 100% 차단.
- 시뮬레이터 콜백 내 `state.subStep === expectedCode` 가드로 오염 방지.

### [INTENT-17] 🔑 2대 교사 마스터 비밀번호 동시 지원 (`260523`, `260831`)
- 학번/비밀번호 로그인 및 `#secure-password-modal`에서 `260523`과 `260831` 모두 정상적으로 교사 관리자 권한을 획득하고 전 서브스텝이 프리패스 해금되어야 함.

---


### [INTENT-RULE10] 📄 소단원 '스스로 확인하기' 1문항 1페이지 원칙
- 각 소단원 말미의 '스스로 확인하기'에 수록된 모든 문항은 결코 한 화면에 병합하지 않고, 문제 1개당 독립된 1개 서브스텝(1페이지)으로 분할하여 전용 시뮬레이터와 채점 폼을 배정해야 함.

### [INTENT-RULE11] 🏆 대단원 '스스로 마무리하기' 전용 대주제 탭 및 1문항 1페이지 원칙
- 대단원 권말의 '스스로 마무리하기'는 타 소단원과 섞이지 않고 별도의 독립 대주제 탭으로 단독 편성해야 함.
- 수록된 모든 문항을 문제 1개당 1페이지(1 서브스텝)로 1:1 완전 매핑해야 하며, 창의융합 프로젝트는 후속 탭으로 분리해야 함.

## 📊 3. 서브에이전트 통과 및 판정 기준 (8대 무조건 반려 원칙)

서브에이전트는 다음 8대 필수 탈락 기준 중 **단 하나라도 위반 시 무조건 반려(`REJECT`)**합니다:

1. **[REJECT-01] 학생 로그인 실패**:
   - 학번 `10101`, 이름 입력 후 활동 뷰로 정상 전환되지 않거나 `0-1`이 열리지 않을 때.
2. **[REJECT-02] 2대 교사 마스터 비밀번호(260523, 260831) 및 모달 실패**:
   - `260523` 또는 `260831` 마스터 비밀번호 바이패스 실패 시.
   - `🔑 교사 계정 접속` 버튼 클릭 시 모달 미출현 또는 모달 내 인증 실패 시.
3. **[REJECT-03] 정답 미노출 원칙 위반 (Zero Answer Leakage)**:
   - `placeholder` 또는 안내 라벨에 실제 문제의 정답이 직접 노출되었을 때.
4. **[REJECT-04] 좌측 캔버스 비어있음 (Blank Canvas)**:
   - 2단원 19개 서브스텝 중 단 하나의 서브스텝이라도 캔버스 함수 미실행 또는 런타임 오류로 빈 화면이 발생할 때.
5. **[REJECT-05] 화면 고정 버그 (Screen Freeze) 및 잔류 애니메이션**:
   - 시뮬레이터 조작 후 서브스텝 이동 시 좌측 캔버스가 이전 단계 화면으로 고정될 때.
   - `loadSubStep` 내 `activeLerpAnimations` 취소 로직 누락 시.
6. **[REJECT-06] 소단원 '스스로 확인하기' 1문항 1페이지 원칙 위반**:
   - 각 소단원 말미의 '스스로 확인하기' 문항들을 한 화면에 묶어 작성하고 1문제당 1페이지로 독립 분할하지 않았을 때.
7. **[REJECT-07] 대단원 '스스로 마무리하기' 전용 탭 미분리 및 1문항 1페이지 원칙 위반**:
   - 대단원 권말 '스스로 마무리하기'가 별도 탭으로 분리되지 않았거나, 전 문항 1문항 1페이지로 분할되지 않았을 때.
8. **[REJECT-08] 질적 완성도 90% 미달**:
   - `g1_coordinate.html` 대비 Two.js 인터랙션, KaTeX 수식, 모달 관제 등 종합 질적 지표가 90% 미만일 때.

### [종합 합격 기준]
- 8대 필수 항목 100% 통과 및 전체 검증 항목 90% 이상 성공 시 최종 합격(`PASS`), 미달 시 반려(`REJECT`).
- 평가 결과는 `docs/eval_ch2_report.md`에 항목별 성공/실패 여부와 종합 점수로 기록.

